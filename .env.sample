GEMINI_API_KEY=
FAL_KEY=

# Topic for the movie (overridden by docker-compose.yml)
TOPIC=Beautiful friendship story

# Playback speed (1.0 = normal, 1.5 = 1.5x faster) (overridden by docker-compose.yml)
PLAYBACK_SPEED=1.5

# Test mode (true/false) (overridden by docker-compose.yml)
TEST_MODE=false